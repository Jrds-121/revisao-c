/* 
Resposta do exercício 7:

A:	"r" Abre o arquivo pra leitura, ele deve exisir.

	"w" Abre o aquivo pra escrita, se o arquivo não existir ele é criado, 
	se ja existir é apagado.

	"a" Abre o arquivo pra escrita, se o arquivo não existir ele é criado,
	se ja existir os dados são adicionados no final.
	
	"r+" Abre o arquivo pra leitura e escrita, ele deve exisir.

	"w+" Abre o arquivo pra leitura e escrita, se o arquivo não existir ele é criado, 
	se ja existir é apagado.

	"a+" Abre o arquivo pra leitura e escrita, se o arquivo não existir ele é criado,
	se ja existir os dados são adicionados no final.


B: fgets() Lê uma linha de texto de um arquivo.

   fprintf() Escreve dados formatados em um arquivo (tipo printf).
   
   fread() Lê dados binários de um arquivo.
   
   fwrite() Escreve dados binários em um arquivo.
*/
